<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    >
    <ImageView android:id="@+id/backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"
        />
    <ImageView android:id="@+id/image"
        android:layout_width="@dimen/panelButtonMediumIcon"
        android:layout_height="@dimen/panelButtonMediumIcon"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:paddingTop="2dp"
        />
    <ImageView android:id="@+id/smallImage"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:adjustViewBounds="true"
        android:layout_alignBottom="@id/image"
        android:layout_alignLeft="@id/image"
        />
    <TextView android:id="@+id/normalText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:lines="2"
        android:maxLines="2"
        android:textColor="#ffffff"
        android:gravity="center_horizontal"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:textSize="12sp"
        android:layout_below="@id/image"
        />
    </RelativeLayout>
